---
- name: Remove apps
  become: true
  apt:
    name:
      - libplank-common
    state: absent
    purge: true

- name: Cleanup config directory
  file:
    path: "{{ home }}/.config/plank"
    state: absent
